when i tried to do maven clean install a jar was generated by  the name which we provided in pom.xml
based on final name.

how we can create the docker image based on jar available in classpath target folder

first with below command we can create the image
docker build -t nut-docker.jar .

to run the image we can give comand like

docker run -p 8085:8081 nut-docker.jar
